# my-react-app

React First Component

This project is a basic React app created using Vite.
It includes a component called FirstComponent that displays the text:

“This is my first React component”

The component is imported and rendered inside App.jsx.

To run the project:

npm install
npm run dev